{
  "$defs": {
    "SourceProvider": {
      "enum": [
        "coros",
        "fit",
        "keep",
        "manual",
        "fixture"
      ],
      "title": "SourceProvider",
      "type": "string"
    }
  },
  "additionalProperties": false,
  "properties": {
    "week_start": {
      "format": "date",
      "title": "Week Start",
      "type": "string"
    },
    "as_of": {
      "format": "date-time",
      "title": "As Of",
      "type": "string"
    },
    "lookback_days": {
      "default": 28,
      "maximum": 56,
      "minimum": 14,
      "title": "Lookback Days",
      "type": "integer"
    },
    "provider": {
      "anyOf": [
        {
          "$ref": "#/$defs/SourceProvider"
        },
        {
          "type": "null"
        }
      ],
      "default": null
    }
  },
  "required": [
    "week_start",
    "as_of"
  ],
  "title": "网页周计划草案输入",
  "type": "object"
}
