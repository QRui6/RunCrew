# ADR-0002：同时保存原始数据与统一 Schema

- 状态：接受
- 日期：2026-08-08

## 背景

如果只保存解析后的字段，Parser 修复或 Schema 升级时无法重新处理历史数据，也难以证明结论使用了什么输入。

## 决策

使用 `raw_provider_events` 保存 Provider 原始返回，使用 `activities` 保存 RunCrew 规范化模型，并通过 hash 和来源 ID 关联。

## 后果

- 支持重放、重新解析和审计；
- 占用更多存储；
- 原始数据的隐私保护要求更高；
- 未来需要数据保留和删除策略。

