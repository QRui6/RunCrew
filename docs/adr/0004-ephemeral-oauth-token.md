# ADR-0004：当前不持久化 OAuth Token

- 状态：接受，临时
- 日期：2026-08-08

## 背景

保存 Refresh Token 可以减少重复授权，但明文保存会扩大私人运动数据泄露风险。项目尚未实现系统凭据存储和撤销机制。

## 决策

当前 Access Token 只保存在同步进程内，进程结束后消失。每次真实 COROS 同步重新授权。

## 后果

- 安全边界简单；
- 本地体验较繁琐；
- 后续实现加密凭据存储前必须新增 ADR 和安全测试。

