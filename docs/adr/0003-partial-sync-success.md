# ADR-0003：详情失败不回滚活动列表

- 状态：接受
- 日期：2026-08-08

## 背景

真实测试中 COROS 活动列表正常，但详情和分圈工具返回服务端异常。如果使用整批事务，详情失败会丢掉已经成功获取的活动摘要。

## 决策

活动列表先独立提交；详情逐条补全。详情失败时：

- 保留 summary；
- 记录 `detail_errors`；
- 同步状态设为 `completed_with_warnings`；
- 不创建虚假的 detail。

## 后果

系统支持部分成功，但消费者必须根据数据类型和 data quality 判断能否进行某项分析。

